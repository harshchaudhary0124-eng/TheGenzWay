"use client";

export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center" style={{ background: "#080808", color: "#F5F2EB" }}>
      <h1 style={{ fontFamily: "'Anton', Impact, sans-serif", fontSize: "clamp(2rem, 6vw, 4rem)" }}>
        Welcome Home
      </h1>
    </main>
  );
}
